#include "main.h"
/**
* _isalpha : function that checks the alphabetic character
* @c: is the function that is used for the argument
* Return: 0
*/
int _isalpha(int c)
{
if ((c >= Ã 'a'&& c <= 'z') || (c >= 'A' && c <= 'Z'))
{
return (1);
}
else
return (0);
}
