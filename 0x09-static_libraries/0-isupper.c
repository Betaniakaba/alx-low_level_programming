#include "main.h"

/**
 * _isupper - function that checks for upeercase characters
 * @c: the integer used for compilation
 * Return: 1
 */
int _isupper(int c)

{

if ((c >= 'A') && (c <= 'Z'))

	{

	return (1);

	}

return (0);

}
