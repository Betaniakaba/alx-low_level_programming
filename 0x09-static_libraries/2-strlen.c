#include "main.h"
#include <string.h>

/**
 * _strlen - returns the strength of a string
 * @s: is used to calculate the value
 * Return: length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}
