#include "main.h"

/**
 * print_rev - a function that prints in reverse
 * @s: integer used
 * Return: void
 */
void print_rev(char *s)
{
	int a = 0;

	while (*(str + a))
	{
	_putchar(*(str + a));
	a--;
	}
	_putchar('\n');
}
